import React from 'react';
// import planet from '../assets/planet.png';

const Header = () => (
  <header>
    <nav>
      <ul>
        <li />
      </ul>
    </nav>
  </header>
);

export default Header;
